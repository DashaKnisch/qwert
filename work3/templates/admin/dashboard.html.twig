<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Админ панель</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            {% if theme == 'dark' %}
                background-color: #1e1e1e;
                color: #f0f0f0;
            {% elseif theme == 'colorblind' %}
                background-color: #ffffe0;
                color: #0000ff;
            {% else %}
                background-color: #ffffff;
                color: #000;
            {% endif %}
            margin: 0;
            padding: 20px;
        }
        
        .header {
            {% if theme == 'dark' %}
                background-color: #2a2a2a;
            {% elseif theme == 'colorblind' %}
                background-color: #ffffc0;
            {% else %}
                background-color: #f0f0f0;
            {% endif %}
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .upload-section {
            margin: 20px 0;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        
        .files-section {
            margin: 20px 0;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        
        .file-item {
            padding: 10px;
            margin: 5px 0;
            {% if theme == 'dark' %}
                background-color: #333;
            {% elseif theme == 'colorblind' %}
                background-color: #fffc80;
            {% else %}
                background-color: #f9f9f9;
            {% endif %}
            border-radius: 3px;
        }
        
        button, .btn {
            {% if theme == 'dark' %}
                background-color: #333;
                color: #fff;
            {% elseif theme == 'colorblind' %}
                background-color: #fffc80;
                color: #0000ff;
            {% else %}
                background-color: #007bff;
                color: white;
            {% endif %}
            padding: 8px 15px;
            border: none;
            border-radius: 3px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }
        
        button:hover, .btn:hover {
            opacity: 0.8;
        }
        
        .message {
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        
        input[type="file"] {
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Админ панель</h1>
        <div>
            <span>Добро пожаловать, <?= $currentUser ?>!</span>
            <a href="/admin/logout" class="btn"><?= $texts['logout'] ?></a>
        </div>
    </div>
    
    <?php if (!empty($message)): ?>
        <div class="message"><?= $message ?></div>
    <?php endif; ?>
    
    <div class="upload-section">
        <h3><?= $texts['upload_pdf'] ?></h3>
        <form method="post" enctype="multipart/form-data">
            <input type="file" name="pdf_file" accept=".pdf" required>
            <br>
            <button type="submit">Загрузить</button>
        </form>
    </div>
    
    <div class="files-section">
        <h3><?= $texts['uploaded_pdf'] ?></h3>
        <?php if (!empty($pdfFiles)): ?>
            <?php foreach ($pdfFiles as $file): ?>
                <div class="file-item">
                    <strong><?= $file['filename'] ?></strong>
                    <a href="/download/<?= $file['id'] ?>" class="btn" style="margin-left: 10px;">Скачать</a>
                </div>
            <?php endforeach; ?>
        <?php else: ?>
            <p>Файлы не найдены</p>
        <?php endif; ?>
    </div>
    
    <div style="margin-top: 30px;">
        <a href="/" class="btn">← Вернуться на главную</a>
    </div>
</body>
</html>